`*.oz` files are compiled using `ozc -c`, then their output is ran using `ozengine`
- both `ozc` and `ozengine` are available through [mozart](https://github.com/mozart/mozart2)
- package available in `yay` for arch
